/*
const nomeMisterios = {
    gozosos: "Mistérios Gozosos",
    luminosos: "Mistérios Luminosos",
    dolorosos: "Mistérios Dolorosos",
    gloriosos: "Mistérios Gloriosos"
}
// Obtém o misterioso selecionado da URL (ex: ?misterio=gozosos e demais)
const parametrosDaURL = new URLSearchParams(window.location.search);
const misterioSelecionado = parametrosDaURL.get('misterio');

if(misterioSelecionado && videosPorMisterio[misterioSelecionado]) {
    // Atualiza o título e o vídeo
    document.getElementById('tituloMisterio').textContent = nomeMisterios[misterioSelecionado];
    // Atualiza o vídeo do iframe
    document.getElementById('videoMisterio').src = videosPorMisterio[misterioSelecionado];
}else{
    // Se o misterioso não for válido, exibe uma mensagem de erro
    document.getElementById('tituloMisterio').textContent = "Misterioso não encontrado";
    
}
*/